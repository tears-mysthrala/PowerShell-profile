# Archive of removed artifacts

This folder documents files that were removed from the main tree because they appear to be
temporary, backup, or leftover artifacts. They were removed to keep the repository clean.


If any removed file is required, restore it from your local history or the previous commit.

Removed files summary:

- Modules/ProfileCore/ProfileCore.psm1.new (empty backup file)
- Modules/ProfileCore/Config/ModuleConfig.psd1.new (backup)
- Modules/ProfileCore/Config/ModuleConfig.psd1.bak (backup)
- .tmp_starship_init_debug.ps1 (temp)
- .tmp_import_profilecore.ps1 (temp)
- .tmp_debug_starship.ps1 (temp)
- .tmp_check_starship.ps1 (temp)

These files were likely created during development or automated updates and are safe to
recreate from the active files in the repository.
